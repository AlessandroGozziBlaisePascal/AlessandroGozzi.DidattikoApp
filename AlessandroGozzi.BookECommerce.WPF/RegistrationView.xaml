<Window x:Class="AlessandroGozzi.BookECommerce.WPF.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registrazione - Used Book Co." Height="600" Width="1000"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Stile TextBox con Watermark (Placeholder) e senza bottoni strani -->
        <Style x:Key="CustomTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#EFE4D3"/>
            <Setter Property="BorderBrush" Value="#D6C4AD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <Grid>
                                <!-- Testo del Placeholder -->
                                <TextBlock x:Name="PlaceholderText"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#888888"
                                           FontSize="18"
                                           VerticalAlignment="Center"
                                           Margin="12,0"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>

                                <!-- Contenitore di testo standard WPF (senza scrollbar orizzontali/bottoni) -->
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stile PasswordBox con Watermark (Placeholder) -->
        <Style x:Key="CustomPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#EFE4D3"/>
            <Setter Property="BorderBrush" Value="#D6C4AD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <Grid>
                                <!-- Testo Placeholder Password -->
                                <TextBlock x:Name="PlaceholderText"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#888888"
                                           FontSize="18"
                                           VerticalAlignment="Center"
                                           Margin="12,0"
                                           IsHitTestVisible="False"/>

                                <ScrollViewer x:Name="PART_ContentHost" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stile Pulsante Verde principale -->
        <Style x:Key="GreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5B8C32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#4A7528"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="4" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Border CornerRadius="12" ClipToBounds="True">
            <Border.Background>
                <ImageBrush ImageSource="/Immagini/SfondoLegnoWPF.jpg" Stretch="UniformToFill"/>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="1" Margin="30,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="1.2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- COLONNA SINISTRA: Divisa in due schede -->
                    <Grid Grid.Column="0" Margin="10,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Scheda Superiore: REGISTRATION -->
                        <Border Grid.Row="0" Margin="0,0,0,10" CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="Black"/>
                            </Border.Effect>
                            <Border CornerRadius="15" ClipToBounds="True">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Immagini/SfondoLegnoChiaroWPF.jpg" Stretch="UniformToFill"/>
                                </Border.Background>
                                <Grid>
                                    <TextBlock Text="REGISTRATION" FontSize="35" FontWeight="Bold" 
                                               Foreground="#5C260B" HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </Border>

                        <!-- Scheda Inferiore: Logo -->
                        <Border Grid.Row="1" Margin="0,10,0,0" CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.4" Color="Black"/>
                            </Border.Effect>
                            <Border CornerRadius="15" ClipToBounds="True">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Immagini/SfondoLegnoChiaroWPF.jpg" Stretch="UniformToFill"/>
                                </Border.Background>
                                <Image Source="/Immagini/LogoDidattiko.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                    </Grid>

                    <!-- COLONNA DESTRA: Form di Registrazione -->
                    <Border Grid.Column="1" Margin="10,5" Background="#FFFBF3" CornerRadius="15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.3" Color="Black"/>
                        </Border.Effect>

                        <Grid Margin="28,22">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Compila per registrarti" FontSize="28" FontWeight="Bold" Foreground="#6A2000" Margin="0,0,0,18"/>

                            <!-- Nome e Cognome -->
                            <Grid Grid.Row="1" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="TxtNome" Tag="Nome" Style="{StaticResource CustomTextBoxStyle}"/>
                                <TextBox Grid.Column="2" x:Name="TxtCognome" Tag="Cognome" Style="{StaticResource CustomTextBoxStyle}"/>
                            </Grid>

                            <!-- Email -->
                            <TextBox Grid.Row="2" x:Name="TxtEmail" Tag="Email" Style="{StaticResource CustomTextBoxStyle}" Margin="0,0,0,12"/>

                            <!-- Telefono e Via/Piazza -->
                            <Grid Grid.Row="3" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="TxtTelefono" Tag="+39 Telefono" Style="{StaticResource CustomTextBoxStyle}"/>
                                <TextBox Grid.Column="2" x:Name="TxtVia" Tag="Via/Piazza" Style="{StaticResource CustomTextBoxStyle}"/>
                            </Grid>

                            <!-- N., Città e CAP -->
                            <Grid Grid.Row="4" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.7*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="TxtCivico" Tag="N." Style="{StaticResource CustomTextBoxStyle}"/>
                                <TextBox Grid.Column="2" x:Name="TxtCitta" Tag="Città" Style="{StaticResource CustomTextBoxStyle}"/>
                                <TextBox Grid.Column="4" x:Name="TxtCap" Tag="CAP" Style="{StaticResource CustomTextBoxStyle}"/>
                            </Grid>

                            <!-- Password e Conferma Password -->
                            <Grid Grid.Row="5" Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="TxtPassword" Tag="Password" Style="{StaticResource CustomTextBoxStyle}"/>
                                <TextBox Grid.Column="2" x:Name="TxtConfermaPassword" Tag="Conferma password" Style="{StaticResource CustomTextBoxStyle}"/>
                            </Grid>

                            <!-- Pulsante Registrati -->
                            <Button Grid.Row="6" x:Name="BtnRegistrati" Content="Registrati" Style="{StaticResource GreenButtonStyle}" Click="BtnRegistrati_Click"/>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>